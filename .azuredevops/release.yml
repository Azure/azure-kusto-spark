# Temporary Azure Pipelines bootstrap for .github/workflows/release.yml

trigger:
- master

pr:
- master

pool:
  vmImage: ubuntu-latest

steps:
- script: echo "Hello from Azure Pipelines"
  displayName: Hello world
